# Two paths, one has extra logic to provide information
# about the data that is expected by the profile, the other
# doesn't need it.

# Things to keep in mind: this is python-only, no db involved
# except for within the populate keyword.

# The user must rewrite the datapoint class


def test_datapoint_init():
    assert False


def test_datapoint_populate():
    assert False


def test_section_init():
    assert False


def test_section_populate():
    assert False


def test_profile_init():
    assert False


def test_profile_populate():
    assert False


